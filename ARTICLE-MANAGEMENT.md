# 文章管理（7.8.4）

在右键菜单、发布进度面板或命令面板打开「文章管理」。可搜索文章，并筛选已发布、已隐藏、已删除和处理中。列表来自已配置的网站仓库及公开网站，不扫描Obsidian笔记库。

- 隐藏：自动提交指定文章的状态申请，检查通过后部署；文章列表和直接链接均不可访问。原稿和仓库Markdown保留。
- 恢复公开：移除隐藏标记；已删除文章可用保留的Git版本恢复。若重新上传了新内容，则恢复最新上传版本。draft仍为true时，先改原稿并上传。
- 删除线上文章：先显示具体标题和slug供确认，只删除对应网站Markdown文件，保留Obsidian原稿和共享附件。检查、合并、部署、索引与404核验自动完成。
- 正文更新：原slug再次发布会更新内容；不会取消独立的隐藏/删除状态。恢复公开必须明确执行管理操作。
- 网络中断：未完成提交的申请可点「继续提交」，复用原分支与申请；已经提交则从发布进度查看或重试。

## 网站协议

使用已有仓库和Token的Contents、Pull requests、Actions读写权限。网站需要支持.publishing/article-state.json及管理申请门禁。该私有文件格式为version:1、articles对象；以英文slug为键，每条包含state(hidden/deleted)、title及恢复用blob。此文件不能放入公开网站产物。

管理分支为enveloppe-manage-操作-随机标识；申请标题Enveloppe: 操作 slug，JSON正文只包含enveloppe:1、action、slug和source blob。云端必须验证同仓库所有者、真实合并树、唯一目标、原文件版本和当前检查，再决定合并；插件不直接绕过门禁合并。

隐藏/删除后，网站必须排除文章路由、列表、搜索、订阅源和站点地图。插件确认公开版本包含本次合并，搜索不含目标且直接链接返回404/410，才显示操作完成。共享附件不删除；文章下架不等于附件访问权限管理。

当前支持默认仓库、最多200篇顶层英文slug的.md文章；多仓库、子目录、MDX和本地文件删除监听不在本版本范围。面板手动刷新列表，进度面板沿用自动轮询。网站流程配置不匹配时停止操作。

## 验证

27项本地行为/API测试、类型检查和生产构建通过，覆盖指定文章删除、原版本恢复、新版本拒绝、未知网络结果恢复提交、旧请求重试，以及公开索引与直接URL同时核验。真实生命周期验收以发行记录为准，模拟测试不等于真实部署。
